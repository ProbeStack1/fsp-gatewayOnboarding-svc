package com.probestack.forgesphere.onboarding.dto;

import lombok.Data;

@Data
public class SubmitApprovalRequest {
    // currently empty because frontend just sends click action.
    // token generation and status update is handled server-side.
}

